import React, { Component } from 'react';
import App from "../../App";
import AppBar from 'material-ui/AppBar';
class Chat extends Component{
    render(){
        return(
            <div>
                <AppBar title={'Chat' }/>
            </div>
        )
    }
}
export default Chat;